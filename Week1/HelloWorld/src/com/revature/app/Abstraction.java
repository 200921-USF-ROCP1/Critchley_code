package com.revature.app;

public interface Abstraction {
	// should not have method bodies
	// just defines what an obj. should look like
	
	public int add(int a, int b);
	
	public int subtract(int a, int b);
}
